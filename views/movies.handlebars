<html>
     <div class="col-12 d-flex justify-content-center text-center">
            <div>
                <h1>MOVIES</h1>
            </div>
        </div>
        <div class="container">
            <div class="text-center">
                <ul id = movies>
                    {{#each movie}}
                        <li>{{title}}</li>
                    {{/each}}
                </ul>
            </div>
        </div>
</html>
 